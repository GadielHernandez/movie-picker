---
interface Props {
    relevant?: boolean;
}

const { relevant = false } = Astro.props;

const clases = `overflow-hidden row-span-1 ${relevant && 'col-span-2'} `;
---
<article class={clases}>
    <slot />
</article>