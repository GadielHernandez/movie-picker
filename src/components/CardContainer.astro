---
interface Props {
    width?: number;
}

const { width = 1 } = Astro.props;

const clases = `rounded-lg shadow-lg overflow-hidden row-span-1 col-span-${width} p-6`;
---
<article class={clases}>
    <slot />
</article>